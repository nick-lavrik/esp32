#pragma once

// Проста точка з координатами. Використовується як для сирих (raw) координат
// з тачскріна, так і для координат екрана - конкретне значення полів
// залежить від контексту виклику (наприклад, TouchPoint до і після
// CoordinateMapper::map()).
struct TouchPoint {
  int x = 0;
  int y = 0;
};

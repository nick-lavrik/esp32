#pragma once

#include <functional>

// Тип колбека завдання. std::function дозволяє передавати як звичайні
// функції, так і лямбди із захопленням (closures).
// УВАГА щодо memory leaks: якщо лямбда захоплює вказівник на об'єкт,
// створений через `new`, звільняти його — відповідальність користувача.
// Рекомендація: захоплювати об'єкти по значенню, через std::shared_ptr,
// або гарантувати, що об'єкт живе довше за завдання.
using TaskCallback = std::function<void()>;
